basePath: /api
definitions:
  handlers.CategoryInfo:
    properties:
      display_name:
        type: string
      name:
        type: string
      type_count:
        type: integer
    type: object
  handlers.ExecuteCommandRequest:
    properties:
      action:
        type: string
      payload:
        additionalProperties: true
        type: object
      target:
        type: string
    required:
    - action
    type: object
  handlers.ExecuteCommandResponse:
    properties:
      data:
        additionalProperties: true
        type: object
      message:
        type: string
      success:
        type: boolean
      timestamp:
        type: string
    type: object
  handlers.ExecuteControlRequest:
    properties:
      value:
        description: The value from the control (bool, number, string, or null for
          buttons)
    type: object
  handlers.ListCategoriesResponse:
    properties:
      categories:
        items:
          $ref: '#/definitions/handlers.CategoryInfo'
        type: array
    type: object
  handlers.ListConnectionTypesResponse:
    properties:
      categories:
        items:
          type: string
        type: array
      count:
        type: integer
      types:
        items:
          $ref: '#/definitions/registry.TypeInfo'
        type: array
    type: object
  handlers.LoginRequest:
    properties:
      key:
        description: User GUID/key
        type: string
    required:
    - key
    type: object
  handlers.LoginResponse:
    properties:
      can_design:
        type: boolean
      can_manage:
        type: boolean
      capabilities:
        items:
          type: string
        type: array
      email:
        type: string
      guid:
        type: string
      name:
        type: string
    type: object
  handlers.SaveSessionRequest:
    properties:
      name:
        type: string
    type: object
  handlers.StreamAggregatedRequest:
    properties:
      function:
        description: avg, min, max, sum, count
        type: string
      interval:
        description: Bucket interval in seconds
        type: integer
      series_col:
        description: Column to partition by (e.g., "location") - optional
        type: string
      timestamp_col:
        description: Timestamp column
        type: string
      value_cols:
        description: Columns to aggregate
        items:
          type: string
        type: array
    required:
    - function
    - interval
    - timestamp_col
    - value_cols
    type: object
  mcp.JSONRPCError:
    properties:
      code:
        type: integer
      data: {}
      message:
        type: string
    type: object
  mcp.JSONRPCRequest:
    properties:
      id: {}
      jsonrpc:
        type: string
      method:
        type: string
      params:
        additionalProperties: true
        type: object
    type: object
  mcp.JSONRPCResponse:
    properties:
      error:
        $ref: '#/definitions/mcp.JSONRPCError'
      id: {}
      jsonrpc:
        type: string
      result: {}
    type: object
  models.AIMessage:
    description: A message in the AI conversation
    properties:
      content:
        type: string
      id:
        type: string
      role:
        description: '"user" | "assistant" | "system"'
        type: string
      timestamp:
        type: string
      tool_calls:
        items:
          $ref: '#/definitions/models.ToolCall'
        type: array
    type: object
  models.AISession:
    description: AI session for chart creation/editing
    properties:
      chart_id:
        description: Chart UUID being edited
        type: string
      chart_version:
        description: Version being edited (always a draft)
        type: integer
      created:
        type: string
      id:
        description: UUID
        type: string
      messages:
        description: Conversation history
        items:
          $ref: '#/definitions/models.AIMessage'
        type: array
      status:
        description: '"active" | "completed" | "cancelled"'
        type: string
      updated:
        type: string
    type: object
  models.AISessionResponse:
    description: Response containing AI session state
    properties:
      chart:
        allOf:
        - $ref: '#/definitions/models.Chart'
        description: Current chart state (draft)
      session:
        $ref: '#/definitions/models.AISession'
    type: object
  models.APIConfig:
    properties:
      auth_credentials:
        additionalProperties:
          type: string
        type: object
      auth_type:
        description: none, bearer, basic, api-key
        type: string
      body:
        description: Request body template
        type: string
      headers:
        additionalProperties:
          type: string
        description: Request headers
        type: object
      method:
        description: HTTP method (GET, POST, etc.)
        type: string
      query_params:
        additionalProperties:
          type: string
        description: Query parameters
        type: object
      response_config:
        allOf:
        - $ref: '#/definitions/models.APIResponseConfig'
        description: Response parsing config
      retry_count:
        type: integer
      retry_delay:
        description: milliseconds
        type: integer
      timeout:
        description: seconds
        type: integer
      url:
        description: Full API endpoint URL
        type: string
    required:
    - url
    type: object
  models.APIResponseConfig:
    properties:
      data_path:
        description: |-
          DataPath is the JSON path to the array of records (e.g., "data", "results", "items")
          If empty, assumes response is already an array or will be parsed as key-value pairs
        type: string
    type: object
  models.CSVConfig:
    properties:
      columns:
        description: explicit column names
        items:
          type: string
        type: array
      delimiter:
        description: 'default: ","'
        type: string
      encoding:
        description: utf-8, ascii, etc.
        type: string
      has_header:
        description: 'default: true'
        type: boolean
      path:
        type: string
      watch_changes:
        type: boolean
    required:
    - path
    type: object
  models.Capability:
    enum:
    - view
    - design
    - manage
    type: string
    x-enum-varnames:
    - CapabilityView
    - CapabilityDesign
    - CapabilityManage
  models.Chart:
    description: Chart/Control with data source binding, query config, and visualization
      settings
    properties:
      ai_session_id:
        description: Active AI session (drafts only)
        type: string
      chart_type:
        description: bar, line, pie, gauge, etc. (charts only)
        type: string
      component_code:
        description: React component code
        type: string
      component_type:
        description: '"chart" (default) | "control"'
        type: string
      connection_id:
        description: Reference to connection
        type: string
      control_config:
        allOf:
        - $ref: '#/definitions/models.ControlConfig'
        description: Control configuration (controls only)
      created:
        type: string
      data_mapping:
        allOf:
        - $ref: '#/definitions/models.ChartDataMapping'
        description: How to map data to chart (charts only)
      description:
        type: string
      id:
        description: UUID - same across versions
        type: string
      name:
        description: Unique identifier
        type: string
      options:
        additionalProperties: true
        description: ECharts options overrides (charts only)
        type: object
      query_config:
        allOf:
        - $ref: '#/definitions/models.ChartQueryConfig'
        description: How to query data (charts only)
      status:
        description: '"draft" | "final"'
        type: string
      tags:
        description: Searchable tags
        items:
          type: string
        type: array
      thumbnail:
        description: Base64 preview image for card display
        type: string
      title:
        description: Display title (defaults to Name if empty)
        type: string
      updated:
        type: string
      use_custom_code:
        description: Whether custom code mode is enabled
        type: boolean
      version:
        description: Version number (1, 2, 3...)
        type: integer
    required:
    - name
    type: object
  models.ChartDataMapping:
    description: Mapping configuration from data columns to chart axes/series
    properties:
      aggregation:
        allOf:
        - $ref: '#/definitions/models.DataAggregation'
        description: Aggregation to apply (first, last, avg, etc.)
      column_aliases:
        additionalProperties:
          type: string
        description: Display names for columns (column name -> display name), primarily
          for dataview
        type: object
      filters:
        description: Client-side filters applied after data fetch
        items:
          $ref: '#/definitions/models.DataFilter'
        type: array
      group_by:
        description: Column to group/split series by (client-side grouping)
        type: string
      label_col:
        description: Column for labels
        type: string
      limit:
        description: Max rows to return
        type: integer
      series:
        description: Column that identifies each series (e.g., "location") - used
          for time bucket partitioning
        type: string
      sliding_window:
        allOf:
        - $ref: '#/definitions/models.SlidingWindow'
        description: Time-based sliding window (e.g., last 5 minutes)
      sort_by:
        description: Column to sort by
        type: string
      sort_order:
        description: asc or desc
        type: string
      time_bucket:
        allOf:
        - $ref: '#/definitions/models.TimeBucket'
        description: Time-bucketed aggregation for streaming data
      x_axis:
        description: Column for X axis (categories)
        type: string
      x_axis_format:
        description: 'Format for X axis values: chart, chart_time, chart_date, chart_datetime,
          short, long, etc.'
        type: string
      x_axis_label:
        description: Label for X axis (e.g., "Time", "Date")
        type: string
      y_axis:
        description: Columns for Y axis (values/series)
        items:
          type: string
        type: array
      y_axis_label:
        description: Label for Y axis (e.g., "Temperature (°F)", "Count")
        type: string
    type: object
  models.ChartListResponse:
    description: Response containing a list of charts with pagination
    properties:
      charts:
        items:
          $ref: '#/definitions/models.Chart'
        type: array
      page:
        type: integer
      page_size:
        type: integer
      total:
        type: integer
    type: object
  models.ChartQueryConfig:
    description: Query configuration for fetching chart data
    properties:
      params:
        additionalProperties: true
        description: Query parameters
        type: object
      raw:
        description: SQL query, filter, or API path
        type: string
      type:
        description: sql, csv_filter, stream_filter, api
        type: string
    type: object
  models.ChartSummary:
    description: Minimal chart info for selection cards and lists
    properties:
      chart_type:
        type: string
      component_type:
        type: string
      connection_id:
        type: string
      description:
        type: string
      id:
        type: string
      name:
        type: string
      status:
        type: string
      tags:
        items:
          type: string
        type: array
      thumbnail:
        type: string
      version:
        type: integer
    type: object
  models.ChartVersionInfo:
    description: Version info for a chart
    properties:
      has_draft:
        description: Whether a draft version exists
        type: boolean
      id:
        type: string
      status:
        type: string
      version:
        type: integer
      version_count:
        description: Total versions for this chart id
        type: integer
    type: object
  models.ColumnInfo:
    properties:
      default:
        description: Default value if any
        type: string
      name:
        description: Column name
        type: string
      nullable:
        description: Whether column allows NULL
        type: boolean
      primary_key:
        description: Whether column is part of primary key
        type: boolean
      type:
        description: Data type (e.g., "varchar", "integer")
        type: string
    type: object
  models.CommandConfig:
    description: Configuration for sending commands via a connection
    properties:
      action:
        description: Command action name
        type: string
      payload_template:
        additionalProperties: true
        description: Template with {{value}} placeholder
        type: object
      target:
        description: Optional target identifier
        type: string
    type: object
  models.CommandDef:
    description: Template for formatting control commands
    properties:
      template:
        additionalProperties: true
        description: Message template with {{value}}, {{target}} placeholders
        type: object
      value_map:
        additionalProperties: true
        description: Optional value mapping (e.g., true -> "ON", false -> "OFF")
        type: object
    type: object
  models.ConfigItem:
    properties:
      category:
        description: Grouping category (e.g., "layout")
        type: string
      created:
        type: string
      description:
        description: Human-readable description
        type: string
      id:
        description: Unique key (e.g., "layout_dimensions")
        type: string
      key:
        description: Same as ID, for clarity
        type: string
      updated:
        type: string
      value:
        description: The setting value
    type: object
  models.ControlConfig:
    description: Configuration for interactive control components (buttons, toggles,
      etc.)
    properties:
      command_config:
        allOf:
        - $ref: '#/definitions/models.CommandConfig'
        description: Legacy command configuration (deprecated, use schema instead)
      control_type:
        description: button, toggle, slider, text_input
        type: string
      schema_id:
        description: Schema-based control configuration (preferred - Phase 3)
        type: string
      target:
        description: Device/endpoint identifier for schema interpolation
        type: string
      ui_config:
        additionalProperties: true
        description: UI configuration
        type: object
    type: object
  models.ControlSchema:
    description: Schema that defines how controls communicate with connections
    properties:
      commands:
        additionalProperties:
          $ref: '#/definitions/models.CommandDef'
        description: Command definitions keyed by control type
        type: object
      created:
        type: string
      description:
        description: Schema description
        type: string
      id:
        description: Unique identifier (e.g., "json-rpc-switch")
        type: string
      is_built_in:
        description: True for system default schemas (cannot modify/delete)
        type: boolean
      metadata:
        additionalProperties: true
        description: Additional custom metadata
        type: object
      name:
        description: Human-readable name
        type: string
      protocol_type:
        description: Connection type this works with (e.g., "websocket-json", "mqtt",
          "tcp-json")
        type: string
      response:
        allOf:
        - $ref: '#/definitions/models.ResponseDef'
        description: How to parse responses (optional)
      state_query:
        allOf:
        - $ref: '#/definitions/models.StateQueryDef'
        description: How to query current state (optional)
      supported_types:
        description: Control UI types this schema supports (toggle, scalar, button,
          text)
        items:
          type: string
        type: array
      updated:
        type: string
      version:
        description: Schema version (e.g., "1.0")
        type: string
    required:
    - name
    type: object
  models.ControlSchemaListResponse:
    description: Response containing a list of control schemas
    properties:
      page:
        type: integer
      page_size:
        type: integer
      schemas:
        items:
          $ref: '#/definitions/models.ControlSchema'
        type: array
      total:
        type: integer
    type: object
  models.CreateAISessionRequest:
    description: Request body for creating a new AI session
    properties:
      chart_id:
        description: Existing chart ID to edit (optional, omit for new chart)
        type: string
      initial_message:
        description: First user message (optional)
        type: string
    type: object
  models.CreateChartRequest:
    description: Request body for creating a new chart or control
    properties:
      chart_type:
        type: string
      component_code:
        type: string
      component_type:
        description: '"chart" (default) | "control"'
        type: string
      connection_id:
        type: string
      control_config:
        $ref: '#/definitions/models.ControlConfig'
      data_mapping:
        $ref: '#/definitions/models.ChartDataMapping'
      description:
        type: string
      name:
        type: string
      options:
        additionalProperties: true
        type: object
      query_config:
        $ref: '#/definitions/models.ChartQueryConfig'
      tags:
        items:
          type: string
        type: array
      thumbnail:
        type: string
      title:
        type: string
      use_custom_code:
        type: boolean
    required:
    - name
    type: object
  models.CreateControlSchemaRequest:
    description: Request body for creating a new control schema
    properties:
      commands:
        additionalProperties:
          $ref: '#/definitions/models.CommandDef'
        description: Command definitions
        type: object
      description:
        description: Schema description
        type: string
      id:
        description: Unique identifier
        type: string
      metadata:
        additionalProperties: true
        description: Custom metadata
        type: object
      name:
        description: Human-readable name
        type: string
      protocol_type:
        description: Connection protocol type
        type: string
      response:
        allOf:
        - $ref: '#/definitions/models.ResponseDef'
        description: Response parsing config
      state_query:
        allOf:
        - $ref: '#/definitions/models.StateQueryDef'
        description: State query config
      supported_types:
        description: Supported control types
        items:
          type: string
        type: array
      version:
        description: Schema version
        type: string
    required:
    - commands
    - id
    - name
    - protocol_type
    - supported_types
    type: object
  models.CreateDashboardRequest:
    description: Request body for creating a new dashboard
    properties:
      description:
        type: string
      metadata:
        additionalProperties: true
        type: object
      name:
        type: string
      panels:
        description: Panels with optional chart_id
        items:
          $ref: '#/definitions/models.DashboardPanel'
        type: array
      settings:
        $ref: '#/definitions/models.DashboardSettings'
    required:
    - name
    type: object
  models.CreateDatasourceRequest:
    properties:
      config:
        $ref: '#/definitions/models.DatasourceConfig'
      description:
        type: string
      mask_secrets:
        description: 'If true, secrets are masked in API responses (default: true)'
        type: boolean
      name:
        type: string
      supported_schemas:
        items:
          type: string
        type: array
      tags:
        items:
          type: string
        type: array
      type:
        allOf:
        - $ref: '#/definitions/models.DatasourceType'
        description: 'LEGACY: Keep for backwards compatibility'
      type_config:
        additionalProperties: true
        type: object
      type_id:
        description: 'NEW: Registry-based type system (preferred)'
        type: string
    required:
    - name
    type: object
  models.CreateUserRequest:
    description: Request body for creating a new user
    properties:
      capabilities:
        items:
          $ref: '#/definitions/models.Capability'
        type: array
      email:
        type: string
      name:
        type: string
    required:
    - name
    type: object
  models.Dashboard:
    description: Dashboard with panels that reference standalone charts
    properties:
      created:
        type: string
      description:
        type: string
      id:
        type: string
      metadata:
        additionalProperties: true
        type: object
      name:
        type: string
      panels:
        description: Panels with chart_id references
        items:
          $ref: '#/definitions/models.DashboardPanel'
        type: array
      settings:
        $ref: '#/definitions/models.DashboardSettings'
      thumbnail:
        description: Base64 encoded thumbnail image
        type: string
      updated:
        type: string
    required:
    - name
    type: object
  models.DashboardListResponse:
    description: Response containing a list of dashboards with pagination
    properties:
      dashboards:
        items:
          $ref: '#/definitions/models.Dashboard'
        type: array
      page:
        type: integer
      page_size:
        type: integer
      total:
        type: integer
    type: object
  models.DashboardPanel:
    description: Panel position and size in the grid with optional chart reference
    properties:
      chart_id:
        description: Reference to chart
        type: string
      h:
        type: integer
      id:
        type: string
      w:
        type: integer
      x:
        type: integer
      "y":
        type: integer
    type: object
  models.DashboardSettings:
    description: Dashboard settings and preferences
    properties:
      allow_export:
        type: boolean
      default_view:
        type: string
      is_public:
        type: boolean
      layout_dimension:
        type: string
      refresh_interval:
        type: integer
      theme:
        type: string
      timezone:
        type: string
      title_scale:
        description: Title font scale % (default 100, range 50-200)
        type: integer
    type: object
  models.DashboardSummary:
    description: Dashboard info with optional data source names for display in tiles
    properties:
      created:
        type: string
      datasource_names:
        description: Unique data source names used by charts
        items:
          type: string
        type: array
      description:
        type: string
      id:
        type: string
      name:
        type: string
      panel_count:
        type: integer
      settings:
        $ref: '#/definitions/models.DashboardSettings'
      thumbnail:
        type: string
      updated:
        type: string
    type: object
  models.DashboardSummaryListResponse:
    description: Response containing dashboard summaries with optional data source
      info
    properties:
      dashboards:
        items:
          $ref: '#/definitions/models.DashboardSummary'
        type: array
      page:
        type: integer
      page_size:
        type: integer
      total:
        type: integer
    type: object
  models.DataAggregation:
    description: Aggregation configuration for data transformation
    properties:
      count:
        description: Row count (for limit)
        type: integer
      field:
        description: Column to aggregate (for min/max/avg/sum)
        type: string
      sort_by:
        description: Column to sort by (for first/last)
        type: string
      type:
        description: first, last, min, max, avg, sum, count, limit
        type: string
    type: object
  models.DataFilter:
    description: Filter condition for data transformation
    properties:
      field:
        description: Column name to filter on
        type: string
      op:
        description: 'Operator: eq, neq, gt, gte, lt, lte, contains, in, notIn, isNull,
          isNotNull'
        type: string
      value:
        description: Value to compare against (can be array for 'in' operator)
    type: object
  models.Datasource:
    properties:
      config:
        $ref: '#/definitions/models.DatasourceConfig'
      created_at:
        type: string
      description:
        type: string
      health:
        $ref: '#/definitions/models.HealthInfo'
      id:
        type: string
      mask_secrets:
        description: If true, secrets are masked in API responses
        type: boolean
      name:
        type: string
      supported_schemas:
        description: Control schema support - which control schemas this connection
          supports
        items:
          type: string
        type: array
      tags:
        items:
          type: string
        type: array
      type:
        allOf:
        - $ref: '#/definitions/models.DatasourceType'
        description: 'LEGACY: Keep for backwards compatibility during migration'
      type_config:
        additionalProperties: true
        type: object
      type_id:
        description: |-
          NEW: Registry-based type system (preferred)
          TypeID format: "category.name" (e.g., "db.postgres", "stream.websocket-bidir")
        type: string
      updated_at:
        type: string
    required:
    - name
    type: object
  models.DatasourceConfig:
    properties:
      api:
        $ref: '#/definitions/models.APIConfig'
      csv:
        $ref: '#/definitions/models.CSVConfig'
      edgelake:
        $ref: '#/definitions/models.EdgeLakeConfig'
      prometheus:
        $ref: '#/definitions/models.PrometheusConfig'
      socket:
        $ref: '#/definitions/models.SocketConfig'
      sql:
        $ref: '#/definitions/models.SQLConfig'
      tsstore:
        $ref: '#/definitions/models.TSStoreConfig'
    type: object
  models.DatasourceType:
    enum:
    - sql
    - csv
    - socket
    - api
    - tsstore
    - prometheus
    - edgelake
    type: string
    x-enum-varnames:
    - DatasourceTypeSQL
    - DatasourceTypeCSV
    - DatasourceTypeSocket
    - DatasourceTypeAPI
    - DatasourceTypeTSStore
    - DatasourceTypePrometheus
    - DatasourceTypeEdgeLake
  models.EdgeLakeConfig:
    properties:
      host:
        description: EdgeLake node IP/hostname
        type: string
      port:
        description: 'REST API port (default: 32049)'
        type: integer
      timeout:
        description: 'Request timeout in seconds (default: 20)'
        type: integer
      use_distributed_query:
        description: 'Add "destination: network" header'
        type: boolean
    required:
    - host
    - port
    type: object
  models.HealthInfo:
    properties:
      error_message:
        type: string
      last_check:
        type: string
      last_success:
        type: string
      response_time:
        description: milliseconds
        type: integer
      status:
        $ref: '#/definitions/models.HealthStatus'
    type: object
  models.HealthStatus:
    enum:
    - unknown
    - healthy
    - unhealthy
    - degraded
    type: string
    x-enum-varnames:
    - HealthStatusUnknown
    - HealthStatusHealthy
    - HealthStatusUnhealthy
    - HealthStatusDegraded
  models.LayoutDimensionDTO:
    properties:
      max_height:
        type: integer
      max_width:
        type: integer
      name:
        type: string
    type: object
  models.PrometheusConfig:
    properties:
      password:
        description: Basic auth password (optional)
        type: string
      timeout:
        description: 'Query timeout in seconds (default: 30)'
        type: integer
      url:
        description: Prometheus server URL (e.g., "http://localhost:9090")
        type: string
      username:
        description: Basic auth username (optional)
        type: string
    required:
    - url
    type: object
  models.PrometheusMetricInfo:
    properties:
      help:
        description: Description from metadata
        type: string
      labels:
        description: Labels seen with this metric
        items:
          type: string
        type: array
      name:
        description: Metric name (e.g., "http_requests_total")
        type: string
      type:
        description: 'Metric type: "counter", "gauge", "histogram", "summary"'
        type: string
    type: object
  models.PrometheusSchemaInfo:
    properties:
      labels:
        description: All label names
        items:
          type: string
        type: array
      metrics:
        description: Available metrics
        items:
          $ref: '#/definitions/models.PrometheusMetricInfo'
        type: array
    type: object
  models.Query:
    properties:
      params:
        additionalProperties: true
        description: Query parameters
        type: object
      raw:
        description: Raw query string (SQL, filter expression, etc.)
        type: string
      type:
        allOf:
        - $ref: '#/definitions/models.QueryType'
        description: Query type
    type: object
  models.QueryRequest:
    properties:
      query:
        $ref: '#/definitions/models.Query'
    required:
    - query
    type: object
  models.QueryResponse:
    properties:
      duration:
        description: milliseconds
        type: integer
      error:
        type: string
      result_set:
        $ref: '#/definitions/models.ResultSet'
      success:
        type: boolean
    type: object
  models.QueryType:
    enum:
    - sql
    - csv_filter
    - stream_filter
    - api
    - tsstore
    - prometheus
    - edgelake
    type: string
    x-enum-varnames:
    - QueryTypeSQL
    - QueryTypeCSVFilter
    - QueryTypeStreamFilter
    - QueryTypeAPI
    - QueryTypeTSStore
    - QueryTypePrometheus
    - QueryTypeEdgeLake
  models.ResponseDef:
    description: Configuration for parsing connection responses
    properties:
      error_path:
        description: JSONPath to error message (e.g., "$.error")
        type: string
      state_path:
        description: JSONPath to current state value (e.g., "$.state")
        type: string
      success_path:
        description: JSONPath to success flag (e.g., "$.success")
        type: string
      value_map:
        additionalProperties: true
        description: Reverse mapping (e.g., "ON" -> true, "OFF" -> false)
        type: object
    type: object
  models.ResultSet:
    properties:
      columns:
        description: Column names
        items:
          type: string
        type: array
      metadata:
        additionalProperties: true
        description: Additional metadata
        type: object
      rows:
        description: Data rows
        items:
          items: {}
          type: array
        type: array
    type: object
  models.SQLConfig:
    properties:
      database:
        type: string
      driver:
        enum:
        - postgres
        - mysql
        - sqlite
        - mssql
        - oracle
        type: string
      host:
        type: string
      max_connections:
        type: integer
      options:
        description: Optional connection parameters (e.g., "sslmode=require&connect_timeout=10")
        type: string
      password:
        type: string
      port:
        type: integer
      ssl:
        type: boolean
      timeout:
        description: seconds
        type: integer
      username:
        type: string
    required:
    - driver
    type: object
  models.SchemaInfo:
    properties:
      database:
        description: Current database name
        type: string
      tables:
        description: Tables in the database
        items:
          $ref: '#/definitions/models.TableInfo'
        type: array
    type: object
  models.SchemaResponse:
    properties:
      duration:
        description: milliseconds
        type: integer
      error:
        type: string
      prometheus_schema:
        allOf:
        - $ref: '#/definitions/models.PrometheusSchemaInfo'
        description: For Prometheus datasources
      schema:
        allOf:
        - $ref: '#/definitions/models.SchemaInfo'
        description: For SQL datasources
      success:
        type: boolean
    type: object
  models.SendMessageRequest:
    description: Request body for sending a user message
    properties:
      content:
        description: User message content
        type: string
    required:
    - content
    type: object
  models.SettingsListResponse:
    properties:
      settings:
        items:
          $ref: '#/definitions/models.ConfigItem'
        type: array
    type: object
  models.SlidingWindow:
    description: Time window configuration for limiting data to recent entries
    properties:
      duration:
        description: Window duration in seconds (e.g., 300 = last 5 minutes)
        type: integer
      timestamp_col:
        description: Column containing timestamps
        type: string
    type: object
  models.SocketConfig:
    properties:
      buffer_size:
        description: number of messages to buffer
        type: integer
      headers:
        additionalProperties:
          type: string
        type: object
      message_format:
        description: json, text, binary
        type: string
      parser:
        allOf:
        - $ref: '#/definitions/models.SocketParserConfig'
        description: parser configuration
      ping_interval:
        description: seconds
        type: integer
      protocol:
        enum:
        - tcp
        - udp
        - websocket
        type: string
      reconnect_delay:
        description: milliseconds
        type: integer
      reconnect_on_error:
        type: boolean
      url:
        type: string
    required:
    - protocol
    - url
    type: object
  models.SocketParserConfig:
    properties:
      data_path:
        description: |-
          DataPath is the JSON path to the data payload (e.g., "data", "payload", "message.readings")
          Supports dot notation for nested paths. If empty, treats entire message as the data object.
        type: string
      exclude_fields:
        description: ExcludeFields removes these fields from output
        items:
          type: string
        type: array
      field_mappings:
        additionalProperties:
          type: string
        description: 'FieldMappings renames fields in the output (e.g., {"temp": "temperature",
          "ts": "timestamp"})'
        type: object
      include_fields:
        description: IncludeFields limits output to only these fields (empty = include
          all)
        items:
          type: string
        type: array
      timestamp_field:
        description: 'TimestampField specifies which field contains the timestamp
          (default: use server receive time)'
        type: string
      timestamp_format:
        description: |-
          TimestampFormat is the Go time format string for parsing timestamps (default: RFC3339)
          Common formats: "2006-01-02T15:04:05Z07:00" (RFC3339), "2006-01-02 15:04:05", unix timestamp
        type: string
    type: object
  models.StateQueryDef:
    description: Configuration for querying control state
    properties:
      interval_ms:
        description: Auto-poll interval in milliseconds (0 = manual only)
        type: integer
      template:
        additionalProperties: true
        description: Query message template
        type: object
    type: object
  models.SystemConfigResponse:
    properties:
      config_refresh_interval:
        description: seconds - how often frontend should refresh dashboard/chart config
        type: integer
      default_dimension:
        type: string
      layout_dimensions:
        additionalProperties:
          $ref: '#/definitions/models.LayoutDimensionDTO'
        type: object
      settings:
        additionalProperties: true
        type: object
    type: object
  models.TSStoreConfig:
    properties:
      api_key:
        description: Optional API key for authentication
        type: string
      data_type:
        allOf:
        - $ref: '#/definitions/models.TSStoreDataType'
        description: 'Store data type: json, schema, text (default: json)'
      headers:
        additionalProperties:
          type: string
        description: Additional HTTP headers
        type: object
      host:
        description: Hostname or IP address
        type: string
      port:
        description: Port number
        type: integer
      protocol:
        allOf:
        - $ref: '#/definitions/models.TSStoreProtocol'
        description: 'Protocol: "http" (HTTP/WS) or "https" (HTTPS/WSS)'
        enum:
        - http
        - https
      push:
        allOf:
        - $ref: '#/definitions/models.TSStorePushConfig'
        description: |-
          Push connection configuration for streaming (ts-store v0.2.2+)
          When streaming is enabled, dashboard calls ts-store API to create a push connection
          and ts-store dials out to dashboard's inbound WebSocket endpoint
      store_name:
        description: Name of the store to query
        type: string
      timeout:
        description: 'Request timeout in seconds (default: 30)'
        type: integer
    required:
    - host
    - port
    - protocol
    - store_name
    type: object
  models.TSStoreDataType:
    enum:
    - json
    - schema
    - text
    type: string
    x-enum-comments:
      TSStoreDataTypeJSON: Arbitrary JSON objects
      TSStoreDataTypeSchema: Schema-defined compact JSON
      TSStoreDataTypeText: UTF-8 text
    x-enum-varnames:
    - TSStoreDataTypeJSON
    - TSStoreDataTypeSchema
    - TSStoreDataTypeText
  models.TSStoreProtocol:
    enum:
    - http
    - https
    type: string
    x-enum-comments:
      TSStoreProtocolHTTP: HTTP/WS (unencrypted)
      TSStoreProtocolHTTPS: HTTPS/WSS (encrypted)
    x-enum-varnames:
    - TSStoreProtocolHTTP
    - TSStoreProtocolHTTPS
  models.TSStorePushConfig:
    properties:
      agg_default:
        description: |-
          AggDefault is the default aggregation function for fields not in AggFields
          Options: avg, sum, min, max, first, last, count
        type: string
      agg_fields:
        description: AggFields specifies per-field aggregation functions (e.g., "temp:avg,count:sum")
        type: string
      agg_window:
        description: |-
          AggWindow is the aggregation window duration (e.g., "1m", "5m", "1h")
          When set, records are aggregated over this time window before sending
        type: string
      connection_id:
        description: |-
          ConnectionID stores the active push connection ID returned by ts-store
          This is set when the push connection is created and used to manage/delete it
        type: string
      filter:
        description: Filter is an optional substring filter - only send matching records
        type: string
      filter_ignore_case:
        description: FilterIgnoreCase enables case-insensitive filter matching
        type: boolean
      format:
        description: 'Format specifies the message format: "full" (default) or "compact"
          (for schema stores)'
        type: string
      from:
        description: From is the starting timestamp in nanoseconds (0 = oldest data,
          -1 = current time/realtime only)
        type: integer
    type: object
  models.TableInfo:
    properties:
      columns:
        description: Columns in the table
        items:
          $ref: '#/definitions/models.ColumnInfo'
        type: array
      name:
        description: Table name
        type: string
      schema:
        description: Schema/namespace (e.g., "public" for PostgreSQL)
        type: string
    type: object
  models.TestDatasourceRequest:
    properties:
      config:
        $ref: '#/definitions/models.DatasourceConfig'
      type:
        allOf:
        - $ref: '#/definitions/models.DatasourceType'
        description: 'LEGACY: Keep for backwards compatibility'
      type_config:
        additionalProperties: true
        type: object
      type_id:
        description: 'NEW: Registry-based type system (preferred)'
        type: string
    type: object
  models.TestDatasourceResponse:
    properties:
      data: {}
      message:
        type: string
      response_time:
        description: milliseconds
        type: integer
      status:
        $ref: '#/definitions/models.HealthStatus'
      success:
        type: boolean
    type: object
  models.TimeBucket:
    description: Time bucket configuration for aggregating streaming data into intervals
    properties:
      function:
        description: 'Aggregation function: avg, min, max, sum, count'
        type: string
      interval:
        description: Bucket interval in seconds (e.g., 60 = 1 minute, 3600 = 1 hour)
        type: integer
      timestamp_col:
        description: Column containing timestamps for bucket alignment
        type: string
      value_cols:
        description: Columns to aggregate (numeric values)
        items:
          type: string
        type: array
    type: object
  models.ToolCall:
    description: A tool call made by the AI
    properties:
      id:
        type: string
      input:
        description: JSON string of tool input
        type: string
      name:
        type: string
      output:
        description: JSON string of tool output
        type: string
    type: object
  models.UpdateChartRequest:
    description: Request body for updating an existing chart or control
    properties:
      chart_type:
        type: string
      component_code:
        type: string
      component_type:
        type: string
      connection_id:
        type: string
      control_config:
        $ref: '#/definitions/models.ControlConfig'
      data_mapping:
        $ref: '#/definitions/models.ChartDataMapping'
      description:
        type: string
      name:
        type: string
      options:
        additionalProperties: true
        type: object
      query_config:
        $ref: '#/definitions/models.ChartQueryConfig'
      tags:
        items:
          type: string
        type: array
      thumbnail:
        type: string
      title:
        type: string
      use_custom_code:
        type: boolean
    type: object
  models.UpdateConfigRequest:
    properties:
      settings:
        additionalProperties: true
        type: object
    required:
    - settings
    type: object
  models.UpdateControlSchemaRequest:
    description: Request body for updating an existing control schema
    properties:
      commands:
        additionalProperties:
          $ref: '#/definitions/models.CommandDef'
        type: object
      description:
        type: string
      metadata:
        additionalProperties: true
        type: object
      name:
        type: string
      protocol_type:
        type: string
      response:
        $ref: '#/definitions/models.ResponseDef'
      state_query:
        $ref: '#/definitions/models.StateQueryDef'
      supported_types:
        items:
          type: string
        type: array
      version:
        type: string
    type: object
  models.UpdateDashboardRequest:
    description: Request body for updating an existing dashboard
    properties:
      description:
        type: string
      metadata:
        additionalProperties: true
        type: object
      name:
        type: string
      panels:
        description: Panels with optional chart_id
        items:
          $ref: '#/definitions/models.DashboardPanel'
        type: array
      settings:
        $ref: '#/definitions/models.DashboardSettings'
      thumbnail:
        type: string
    type: object
  models.UpdateDatasourceRequest:
    properties:
      config:
        allOf:
        - $ref: '#/definitions/models.DatasourceConfig'
        description: 'LEGACY: Keep for backwards compatibility'
      description:
        type: string
      mask_secrets:
        description: If provided, updates secret masking setting
        type: boolean
      name:
        type: string
      supported_schemas:
        items:
          type: string
        type: array
      tags:
        items:
          type: string
        type: array
      type_config:
        additionalProperties: true
        type: object
      type_id:
        description: 'NEW: Registry-based type system'
        type: string
    type: object
  models.UpdateSettingRequest:
    properties:
      value: {}
    required:
    - value
    type: object
  models.UpdateUserRequest:
    description: Request body for updating an existing user
    properties:
      active:
        type: boolean
      capabilities:
        items:
          $ref: '#/definitions/models.Capability'
        type: array
      email:
        type: string
      name:
        type: string
    type: object
  models.User:
    description: User account with authentication and capabilities
    properties:
      active:
        description: Whether user is active
        type: boolean
      capabilities:
        description: User capabilities
        items:
          $ref: '#/definitions/models.Capability'
        type: array
      created:
        type: string
      email:
        description: Optional email
        type: string
      guid:
        description: UUID for authentication header
        type: string
      id:
        type: string
      name:
        description: Display name
        type: string
      updated:
        type: string
    type: object
  models.UserCapabilitiesResponse:
    description: Current user's capabilities and access permissions
    properties:
      can_design:
        type: boolean
      can_manage:
        type: boolean
      capabilities:
        items:
          $ref: '#/definitions/models.Capability'
        type: array
      name:
        type: string
      user_id:
        type: string
    type: object
  models.UserConfigResponse:
    properties:
      settings:
        additionalProperties: true
        type: object
      user_id:
        type: string
    type: object
  models.UserListResponse:
    description: Response containing a list of users with pagination
    properties:
      page:
        type: integer
      page_size:
        type: integer
      total:
        type: integer
      users:
        items:
          $ref: '#/definitions/models.User'
        type: array
    type: object
  registry.Capabilities:
    properties:
      can_read:
        description: All adapters support reading
        type: boolean
      can_stream:
        description: Real-time subscription support
        type: boolean
      can_write:
        description: Bidirectional adapters only
        type: boolean
    type: object
  registry.ConfigField:
    properties:
      default: {}
      description:
        type: string
      name:
        type: string
      options:
        description: For select type
        items:
          type: string
        type: array
      required:
        type: boolean
      type:
        description: string, int, bool, password, select
        type: string
    type: object
  registry.TypeInfo:
    properties:
      capabilities:
        $ref: '#/definitions/registry.Capabilities'
      category:
        description: e.g., "db", "stream", "api", "file", "store"
        type: string
      config_schema:
        items:
          $ref: '#/definitions/registry.ConfigField'
        type: array
      display_name:
        type: string
      type_id:
        type: string
    type: object
  streaming.StreamStatus:
    properties:
      bufferCount:
        type: integer
      connected:
        type: boolean
      datasourceID:
        type: string
      lastError: {}
      subscriberCount:
        type: integer
    type: object
host: localhost:3001
info:
  contact:
    email: support@example.com
    name: Dashboard Team
  description: Dashboard system with AI-powered chart generation
  title: TRVE Dashboards API
  version: "1.0"
paths:
  /ai/sessions:
    post:
      consumes:
      - application/json
      description: Create a new AI session for chart creation or editing. Creates
        a chart draft.
      parameters:
      - description: Session creation request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.CreateAISessionRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/models.AISessionResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Create a new AI session
      tags:
      - ai
  /ai/sessions/{id}:
    delete:
      description: Cancel the AI session and discard the draft
      parameters:
      - description: Session ID
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Cancel AI session
      tags:
      - ai
    get:
      description: Get an AI session by ID with current chart state
      parameters:
      - description: Session ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.AISessionResponse'
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Get AI session
      tags:
      - ai
  /ai/sessions/{id}/messages:
    post:
      consumes:
      - application/json
      description: Send a user message to an AI session. The AI agent will process
        the message asynchronously.
      parameters:
      - description: Session ID
        in: path
        name: id
        required: true
        type: string
      - description: Message content
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.SendMessageRequest'
      produces:
      - application/json
      responses:
        "202":
          description: Message accepted for processing
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Send message to AI session
      tags:
      - ai
  /ai/sessions/{id}/save:
    post:
      consumes:
      - application/json
      description: Save the AI session by publishing the draft as a new final version
      parameters:
      - description: Session ID
        in: path
        name: id
        required: true
        type: string
      - description: Chart name
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/handlers.SaveSessionRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Chart'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Save AI session (publish draft)
      tags:
      - ai
  /ai/sessions/{id}/ws:
    get:
      description: Subscribe to real-time updates for an AI session via WebSocket
      parameters:
      - description: Session ID
        in: path
        name: id
        required: true
        type: string
      responses:
        "101":
          description: Switching Protocols
          schema:
            type: string
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
      summary: Subscribe to AI session events via WebSocket
      tags:
      - ai
  /api/ai/debug:
    get:
      description: Stream real-time debug events from the AI agent including LLM requests,
        responses, and tool calls
      produces:
      - application/json
      responses:
        "101":
          description: Switching Protocols
          schema:
            type: string
      summary: Connect to AI debug WebSocket
      tags:
      - debug
  /api/ai/debug/status:
    get:
      description: Get the current status of the AI debug system
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: Get debug status
      tags:
      - debug
  /api/controls/{id}/execute:
    post:
      consumes:
      - application/json
      description: Executes the command configured on a control component, using either
        schema-based or legacy template interpolation
      parameters:
      - description: Control (Chart) ID
        in: path
        name: id
        required: true
        type: string
      - description: Control value
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/handlers.ExecuteControlRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/handlers.ExecuteCommandResponse'
        "400":
          description: Bad request - not a control or missing connection
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Control not found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal server error
          schema:
            additionalProperties: true
            type: object
      summary: Execute a control component command
      tags:
      - controls
  /api/datasources/{id}/command:
    post:
      consumes:
      - application/json
      description: Send a command to a datasource that supports write operations (e.g.,
        stream.websocket-bidir)
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      - description: Command to execute
        in: body
        name: command
        required: true
        schema:
          $ref: '#/definitions/handlers.ExecuteCommandRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/handlers.ExecuteCommandResponse'
        "400":
          description: Bad request - connection does not support write
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Datasource not found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal server error
          schema:
            additionalProperties: true
            type: object
      summary: Execute a command on a bidirectional datasource
      tags:
      - datasources
  /api/registry/categories:
    get:
      description: Get all available categories with their type counts
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/handlers.ListCategoriesResponse'
      summary: List all connection type categories
      tags:
      - registry
  /api/registry/connections:
    get:
      description: Get all registered adapter types with their capabilities and configuration
        schema
      parameters:
      - description: Filter by category (e.g., 'db', 'stream', 'api')
        in: query
        name: category
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/handlers.ListConnectionTypesResponse'
      summary: List all available connection types
      tags:
      - registry
  /api/registry/connections/{typeId}:
    get:
      description: Get details about a specific adapter type including configuration
        schema
      parameters:
      - description: Type ID (e.g., 'db.postgres', 'stream.websocket-bidir')
        in: path
        name: typeId
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/registry.TypeInfo'
        "404":
          description: Type not found
          schema:
            additionalProperties: true
            type: object
      summary: Get a specific connection type
      tags:
      - registry
  /api/settings:
    get:
      consumes:
      - application/json
      description: Get all settings that can be modified by administrators
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.SettingsListResponse'
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get all user-configurable settings
      tags:
      - settings
  /api/settings/{key}:
    get:
      consumes:
      - application/json
      description: Get a single setting by key
      parameters:
      - description: Setting key
        in: path
        name: key
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.ConfigItem'
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get a single setting
      tags:
      - settings
    put:
      consumes:
      - application/json
      description: Update the value of a user-configurable setting
      parameters:
      - description: Setting key
        in: path
        name: key
        required: true
        type: string
      - description: New value for the setting
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/models.UpdateSettingRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.ConfigItem'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Update a setting
      tags:
      - settings
  /auth/login:
    post:
      consumes:
      - application/json
      description: Validates a user key (GUID) and returns user info if valid
      parameters:
      - description: Login request with key
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/handlers.LoginRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/handlers.LoginResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Login with key
      tags:
      - Auth
  /auth/me:
    get:
      description: Returns the authenticated user's ID, name, and capabilities
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.UserCapabilitiesResponse'
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get current user capabilities
      tags:
      - Auth
  /charts:
    get:
      description: Get a paginated list of charts (latest version of each) with optional
        filtering
      parameters:
      - description: Filter by name (partial match)
        in: query
        name: name
        type: string
      - description: Filter by chart type
        in: query
        name: chart_type
        type: string
      - description: Filter by data source ID
        in: query
        name: datasource_id
        type: string
      - description: Filter by tag
        in: query
        name: tag
        type: string
      - default: 1
        description: Page number
        in: query
        name: page
        type: integer
      - default: 20
        description: Page size
        in: query
        name: page_size
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.ChartListResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: List charts
      tags:
      - charts
    post:
      consumes:
      - application/json
      description: Create a new chart with data source binding and visualization config.
        Creates as version 1 with status "final".
      parameters:
      - description: Chart data
        in: body
        name: chart
        required: true
        schema:
          $ref: '#/definitions/models.CreateChartRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/models.Chart'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Create a new chart
      tags:
      - charts
  /charts/{id}:
    delete:
      description: Delete all versions of a chart by ID
      parameters:
      - description: Chart ID
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Delete a chart
      tags:
      - charts
    get:
      description: Get the latest version of a chart by ID
      parameters:
      - description: Chart ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Chart'
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Get a chart
      tags:
      - charts
    put:
      consumes:
      - application/json
      description: Update the latest version of a chart in-place (for manual edits)
      parameters:
      - description: Chart ID
        in: path
        name: id
        required: true
        type: string
      - description: Chart update data
        in: body
        name: chart
        required: true
        schema:
          $ref: '#/definitions/models.UpdateChartRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Chart'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Update a chart
      tags:
      - charts
  /charts/{id}/draft:
    delete:
      description: Delete the draft version of a chart (if exists)
      parameters:
      - description: Chart ID
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Delete chart draft
      tags:
      - charts
    get:
      description: Get the draft version of a chart (if exists)
      parameters:
      - description: Chart ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Chart'
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Get chart draft
      tags:
      - charts
  /charts/{id}/version-info:
    get:
      description: Get version metadata for a chart (version count, has draft, etc.)
      parameters:
      - description: Chart ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.ChartVersionInfo'
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Get chart version info
      tags:
      - charts
  /charts/{id}/versions:
    get:
      description: Get all versions of a chart by ID
      parameters:
      - description: Chart ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: List chart versions
      tags:
      - charts
  /charts/{id}/versions/{version}:
    delete:
      description: Delete a specific version of a chart
      parameters:
      - description: Chart ID
        in: path
        name: id
        required: true
        type: string
      - description: Version number
        in: path
        name: version
        required: true
        type: integer
      responses:
        "204":
          description: No Content
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Delete a chart version
      tags:
      - charts
    get:
      description: Get a specific version of a chart by ID and version number
      parameters:
      - description: Chart ID
        in: path
        name: id
        required: true
        type: string
      - description: Version number
        in: path
        name: version
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Chart'
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Get a specific chart version
      tags:
      - charts
  /charts/summaries:
    get:
      description: Get lightweight chart summaries for card-based selection UI
      parameters:
      - default: 50
        description: Maximum number of summaries
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/models.ChartSummary'
            type: array
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Get chart summaries
      tags:
      - charts
  /config/system:
    get:
      description: Retrieves system-wide configuration including layout dimensions
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.SystemConfigResponse'
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get system configuration
      tags:
      - config
    put:
      consumes:
      - application/json
      description: Updates system-wide configuration settings
      parameters:
      - description: Configuration settings to update
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.UpdateConfigRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.SystemConfigResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Update system configuration
      tags:
      - config
  /config/user/{user_id}:
    get:
      description: Retrieves configuration for a specific user
      parameters:
      - description: User ID
        in: path
        name: user_id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.UserConfigResponse'
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get user configuration
      tags:
      - config
    put:
      consumes:
      - application/json
      description: Updates configuration for a specific user
      parameters:
      - description: User ID
        in: path
        name: user_id
        required: true
        type: string
      - description: Configuration settings to update
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.UpdateConfigRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.UserConfigResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Update user configuration
      tags:
      - config
  /control-schemas:
    get:
      description: List control schemas with optional filtering by protocol type or
        control type
      parameters:
      - description: Filter by protocol type
        in: query
        name: protocol_type
        type: string
      - description: Filter by supported control type
        in: query
        name: control_type
        type: string
      - description: Show only built-in schemas
        in: query
        name: built_in_only
        type: boolean
      - description: Page number (default 1)
        in: query
        name: page
        type: integer
      - description: Page size (default 50, max 100)
        in: query
        name: page_size
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.ControlSchemaListResponse'
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: List control schemas
      tags:
      - control-schemas
    post:
      consumes:
      - application/json
      description: Create a new control schema defining how controls communicate with
        connections
      parameters:
      - description: Schema data
        in: body
        name: schema
        required: true
        schema:
          $ref: '#/definitions/models.CreateControlSchemaRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/models.ControlSchema'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Create a new control schema
      tags:
      - control-schemas
  /control-schemas/{id}:
    delete:
      description: Delete a control schema. Built-in schemas cannot be deleted.
      parameters:
      - description: Schema ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "204":
          description: No Content
        "403":
          description: Forbidden
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Delete a control schema
      tags:
      - control-schemas
    get:
      description: Get a control schema by its ID
      parameters:
      - description: Schema ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.ControlSchema'
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Get a control schema
      tags:
      - control-schemas
    put:
      consumes:
      - application/json
      description: Update an existing control schema. Built-in schemas cannot be modified.
      parameters:
      - description: Schema ID
        in: path
        name: id
        required: true
        type: string
      - description: Schema updates
        in: body
        name: schema
        required: true
        schema:
          $ref: '#/definitions/models.UpdateControlSchemaRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.ControlSchema'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "403":
          description: Forbidden
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Update a control schema
      tags:
      - control-schemas
  /control-schemas/by-control-type/{control_type}:
    get:
      description: Get all control schemas that support a specific control type
      parameters:
      - description: Control type (toggle, scalar, button, text)
        in: path
        name: control_type
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Get schemas for control type
      tags:
      - control-schemas
  /control-schemas/by-protocol/{protocol_type}:
    get:
      description: Get all control schemas compatible with a specific protocol type
      parameters:
      - description: Protocol type (e.g., websocket-json)
        in: path
        name: protocol_type
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Get schemas for protocol
      tags:
      - control-schemas
  /control-schemas/types:
    get:
      description: Get the list of valid control UI types (toggle, scalar, button,
        text)
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: Get valid control types
      tags:
      - control-schemas
  /dashboards:
    get:
      description: Get a paginated list of dashboards with optional filtering. Use
        include_datasources=true to get data source names for each dashboard.
      parameters:
      - description: Filter by name (partial match)
        in: query
        name: name
        type: string
      - description: Filter by public status
        in: query
        name: is_public
        type: boolean
      - description: Include data source names from charts
        in: query
        name: include_datasources
        type: boolean
      - default: 1
        description: Page number
        in: query
        name: page
        type: integer
      - default: 20
        description: Page size
        in: query
        name: page_size
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Response when include_datasources=true
          schema:
            $ref: '#/definitions/models.DashboardSummaryListResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: List dashboards
      tags:
      - dashboards
    post:
      consumes:
      - application/json
      description: Create a new dashboard with panels and embedded charts
      parameters:
      - description: Dashboard data
        in: body
        name: dashboard
        required: true
        schema:
          $ref: '#/definitions/models.CreateDashboardRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/models.Dashboard'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Create a new dashboard
      tags:
      - dashboards
  /dashboards/{id}:
    delete:
      description: Delete a dashboard by ID
      parameters:
      - description: Dashboard ID
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Delete a dashboard
      tags:
      - dashboards
    get:
      description: Get a dashboard by ID (includes panels and charts)
      parameters:
      - description: Dashboard ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Dashboard'
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Get a dashboard
      tags:
      - dashboards
    put:
      consumes:
      - application/json
      description: Update an existing dashboard
      parameters:
      - description: Dashboard ID
        in: path
        name: id
        required: true
        type: string
      - description: Dashboard update data
        in: body
        name: dashboard
        required: true
        schema:
          $ref: '#/definitions/models.UpdateDashboardRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Dashboard'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Update a dashboard
      tags:
      - dashboards
  /datasources:
    get:
      description: Retrieve all datasources with pagination and optional type filter
      parameters:
      - default: 20
        description: Number of items per page
        in: query
        name: limit
        type: integer
      - default: 0
        description: Number of items to skip
        in: query
        name: offset
        type: integer
      - description: Filter by datasource type (api, websocket, file)
        in: query
        name: type
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: List all datasources
      tags:
      - datasources
    post:
      consumes:
      - application/json
      description: Create a new data source (API, WebSocket, or File)
      parameters:
      - description: Datasource to create
        in: body
        name: datasource
        required: true
        schema:
          $ref: '#/definitions/models.CreateDatasourceRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/models.Datasource'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Create a new datasource
      tags:
      - datasources
  /datasources/{id}:
    delete:
      description: Delete a datasource by ID
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
      summary: Delete a datasource
      tags:
      - datasources
    get:
      description: Retrieve a single datasource by its ID
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Datasource'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
      summary: Get a datasource by ID
      tags:
      - datasources
    put:
      consumes:
      - application/json
      description: Update an existing datasource by ID
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      - description: Datasource updates
        in: body
        name: datasource
        required: true
        schema:
          $ref: '#/definitions/models.UpdateDatasourceRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Datasource'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Update a datasource
      tags:
      - datasources
  /datasources/{id}/edgelake/databases:
    get:
      description: Retrieve all database names from an EdgeLake node's blockchain
        registry
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
      summary: Get databases from an EdgeLake data source
      tags:
      - datasources
  /datasources/{id}/edgelake/schema:
    get:
      description: Retrieve column names and types for a specific table from an EdgeLake
        node
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      - description: Database name
        in: query
        name: database
        required: true
        type: string
      - description: Table name
        in: query
        name: table
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
      summary: Get table schema from an EdgeLake data source
      tags:
      - datasources
  /datasources/{id}/edgelake/tables:
    get:
      description: Retrieve table names for a specific database from an EdgeLake node
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      - description: Database name
        in: query
        name: database
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
      summary: Get tables from an EdgeLake data source
      tags:
      - datasources
  /datasources/{id}/health:
    post:
      description: Check the health of a specific datasource and update its status
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.HealthInfo'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
      summary: Check datasource health
      tags:
      - datasources
  /datasources/{id}/prometheus/labels/{label}/values:
    get:
      description: Retrieve all possible values for a specific label from a Prometheus
        datasource
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      - description: Label name
        in: path
        name: label
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
      summary: Get values for a Prometheus label
      tags:
      - datasources
  /datasources/{id}/query:
    post:
      consumes:
      - application/json
      description: Execute a query and return normalized results
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      - description: Query to execute
        in: body
        name: query
        required: true
        schema:
          $ref: '#/definitions/models.QueryRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.QueryResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
      summary: Execute a query against a datasource
      tags:
      - datasources
  /datasources/{id}/schema:
    get:
      description: Retrieve tables and columns for SQL datasources. Only SQL-type
        datasources support this endpoint.
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.SchemaResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
      summary: Get database schema for a SQL datasource
      tags:
      - datasources
  /datasources/{id}/stream:
    get:
      description: Opens an SSE connection to stream real-time data from a socket
        datasource
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - text/event-stream
      responses:
        "200":
          description: SSE stream
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Stream data from a socket datasource
      tags:
      - datasources
  /datasources/{id}/stream/aggregated:
    post:
      consumes:
      - application/json
      description: Opens an SSE connection to stream time-bucketed aggregated data
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      - description: Aggregation configuration
        in: body
        name: config
        required: true
        schema:
          $ref: '#/definitions/handlers.StreamAggregatedRequest'
      produces:
      - text/event-stream
      responses:
        "200":
          description: SSE stream
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Stream aggregated data from a socket datasource
      tags:
      - datasources
  /datasources/{id}/stream/status:
    get:
      description: Get status information for an active stream
      parameters:
      - description: Datasource ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/streaming.StreamStatus'
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
      summary: Get stream status
      tags:
      - datasources
  /datasources/aggregators:
    get:
      description: Get statistics about active bucket aggregators
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: Get aggregator statistics
      tags:
      - datasources
  /datasources/streams:
    get:
      description: Get a list of all active streaming connections
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: List active streams
      tags:
      - datasources
  /datasources/test:
    post:
      consumes:
      - application/json
      description: Test a datasource connection without saving it
      parameters:
      - description: Datasource configuration to test
        in: body
        name: datasource
        required: true
        schema:
          $ref: '#/definitions/models.TestDatasourceRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.TestDatasourceResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
      summary: Test a datasource connection
      tags:
      - datasources
  /health:
    get:
      description: Check if the server and dependencies are healthy
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
        "503":
          description: Service Unavailable
          schema:
            additionalProperties: true
            type: object
      summary: Health check
      tags:
      - System
  /mcp/message:
    post:
      consumes:
      - application/json
      description: Process a JSON-RPC message for MCP protocol
      parameters:
      - description: JSON-RPC request
        in: body
        name: message
        required: true
        schema:
          $ref: '#/definitions/mcp.JSONRPCRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/mcp.JSONRPCResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/mcp.JSONRPCResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/mcp.JSONRPCResponse'
      summary: Handle MCP Message
      tags:
      - MCP
  /mcp/sse:
    get:
      description: Establish an SSE connection for MCP protocol
      produces:
      - text/event-stream
      responses:
        "200":
          description: SSE stream
          schema:
            type: string
      summary: MCP SSE Connection
      tags:
      - MCP
  /users:
    get:
      description: Returns a paginated list of all users
      parameters:
      - default: 1
        description: Page number
        in: query
        name: page
        type: integer
      - default: 10
        description: Page size
        in: query
        name: page_size
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.UserListResponse'
        "403":
          description: Forbidden
          schema:
            additionalProperties:
              type: string
            type: object
      summary: List all users
      tags:
      - Users
    post:
      consumes:
      - application/json
      description: Creates a new user account
      parameters:
      - description: User data
        in: body
        name: user
        required: true
        schema:
          $ref: '#/definitions/models.CreateUserRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/models.User'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Create a new user
      tags:
      - Users
  /users/{id}:
    delete:
      description: Deletes a user account
      parameters:
      - description: User ID
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Delete a user
      tags:
      - Users
    get:
      description: Returns a user by their ID
      parameters:
      - description: User ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.User'
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get user by ID
      tags:
      - Users
    put:
      consumes:
      - application/json
      description: Updates an existing user's information
      parameters:
      - description: User ID
        in: path
        name: id
        required: true
        type: string
      - description: User data
        in: body
        name: user
        required: true
        schema:
          $ref: '#/definitions/models.UpdateUserRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.User'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Update a user
      tags:
      - Users
swagger: "2.0"
